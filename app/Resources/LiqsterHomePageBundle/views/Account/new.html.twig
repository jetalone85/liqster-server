{% extends '@LiqsterHomePage/base.html.twig' %}
{% form_theme form '@LiqsterHomePage/form/fields.html.twig' %}

{% block body %}

    <div class="container-fluid">
        <div class="container">
            <h1>Account creation</h1>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}

            <a class="btn btn-info btn-block" href="{{ path('account_index') }}" role="button">Back to the list</a>
        </div>
    </div>

{% endblock %}
