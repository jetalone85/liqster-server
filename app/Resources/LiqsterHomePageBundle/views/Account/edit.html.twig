{% extends '@LiqsterHomePage/base.html.twig' %}
{% form_theme edit_form '@LiqsterHomePage/form/fields.html.twig' %}
{% form_theme delete_form '@LiqsterHomePage/form/fields.html.twig' %}

{% block body %}

    <div class="container-fluid">
        <div class="container">
            <h1>Account edit</h1>

            {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
            {{ form_end(edit_form) }}

            <a class="btn btn-info btn-block" href="{{ path('account_index') }}" role="button">Back to the
                list</a>
            {{ form_start(delete_form) }}
            {{ form_end(delete_form) }}
            </ul>
        </div>
    </div>

{% endblock %}
